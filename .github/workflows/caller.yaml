name: Caller Workflow testing testing

on:
  workflow_dispatch:
  
  push:
    branches:
      - main

jobs:
  trigger-reusable-workflow:
    uses: viveky20/GHA-Calling-Repo/.github/workflows/called.yaml@main
        
